{
  "name": "@enhancedjs/vue-template-in-string-loader",
  "version": "0.1.5",
  "license": "CC0-1.0",
  "description": "Pre-compile Vue templates in JS template strings at compile time.",
  "author": "EnhancedJS Team",
  "main": "dist/index.js",
  "scripts": {
    "prepublishOnly": "npm run lint && npm run test && npm run build",
    "clear": "rimraf 'dist/*'",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "build": "npm run clear && npm run tsc",
    "lint": "tslint -p tsconfig.json -t verbose",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@vue/component-compiler-utils": "^3.1.2",
    "loader-utils": "^2.0.0",
    "vue-template-compiler": "^2.6.11"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/loader-utils": "^2.0.1",
    "@types/node": "^14.0.13",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.0",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/enhancedjs/vue-template-in-string-loader.git"
  },
  "keywords": [
    "vue",
    "template",
    "string",
    "webpack"
  ]
}
