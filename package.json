{
  "name": "vue-template-in-string-loader",
  "version": "0.1.3",
  "license": "CC0-1.0",
  "description": "Compile Vue templates in template strings at build time.",
  "author": "Paroi",
  "main": "dist/index.js",
  "scripts": {
    "prepublishOnly": "npm run lint && npm run test && npm run build",
    "clear": "rimraf 'dist/*'",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "build": "npm run clear && npm run tsc",
    "lint": "tslint -p tsconfig.json -t verbose",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@vue/component-compiler-utils": "^3.1.2",
    "loader-utils": "^2.0.0",
    "vue-template-compiler": "^2.6.11"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@types/loader-utils": "^2.0.0",
    "@types/node": "^14.0.12",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.0",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paroi-tech/vue-template-in-string-loader.git"
  },
  "keywords": [
    "vue",
    "template",
    "string",
    "webpack"
  ]
}
